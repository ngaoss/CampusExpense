<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Manager"
        tools:targetApi="31">
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Manager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- In AndroidManifest.xml -->
        <activity
            android:name=".ReportActivity"
            android:label="Detailed Reports"
        android:parentActivityName=".ConclusionActivity" />
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".ConclusionActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".Budget"
            android:exported="false" />
        <activity
            android:name=".data.AddEditExpenseActivity"
            android:exported="false" />
        <activity
            android:name=".data.AddRecurringExpenseActivity"
            android:exported="false" />
        <activity
            android:name=".data.RecurringRulesActivity"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>